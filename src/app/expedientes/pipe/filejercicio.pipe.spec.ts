import { FilejercicioPipe } from './filejercicio.pipe';

describe('FilejercicioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilejercicioPipe();
    expect(pipe).toBeTruthy();
  });
});
