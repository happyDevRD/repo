import { FilexpedientePipe } from './filexpediente.pipe';

describe('FilexpedientePipe', () => {
  it('create an instance', () => {
    const pipe = new FilexpedientePipe();
    expect(pipe).toBeTruthy();
  });
});
