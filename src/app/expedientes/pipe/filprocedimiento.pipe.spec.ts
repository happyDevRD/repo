import { FilprocedimientoPipe } from './filprocedimiento.pipe';

describe('FilprocedimientoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilprocedimientoPipe();
    expect(pipe).toBeTruthy();
  });
});
