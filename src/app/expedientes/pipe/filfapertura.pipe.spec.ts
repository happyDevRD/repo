import { FilfaperturaPipe } from './filfapertura.pipe';

describe('FilfaperturaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilfaperturaPipe();
    expect(pipe).toBeTruthy();
  });
});
