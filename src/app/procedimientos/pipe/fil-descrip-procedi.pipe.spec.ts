import { FilDescripProcediPipe } from './fil-descrip-procedi.pipe';

describe('FilDescripProcediPipe', () => {
  it('create an instance', () => {
    const pipe = new FilDescripProcediPipe();
    expect(pipe).toBeTruthy();
  });
});
