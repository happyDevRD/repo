import { FilSiaProcediPipe } from './fil-sia-procedi.pipe';

describe('FilSiaProcediPipe', () => {
  it('create an instance', () => {
    const pipe = new FilSiaProcediPipe();
    expect(pipe).toBeTruthy();
  });
});
