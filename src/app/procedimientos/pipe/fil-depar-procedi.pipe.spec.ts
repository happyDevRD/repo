import { FilDeparProcediPipe } from './fil-depar-procedi.pipe';

describe('FilDeparProcediPipe', () => {
  it('create an instance', () => {
    const pipe = new FilDeparProcediPipe();
    expect(pipe).toBeTruthy();
  });
});
