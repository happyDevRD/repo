import { FilasignadoPipe } from './filasignado.pipe';

describe('FilasignadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilasignadoPipe();
    expect(pipe).toBeTruthy();
  });
});
