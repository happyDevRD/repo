import { FilinteresadoPipe } from './filinteresado.pipe';

describe('FilinteresadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilinteresadoPipe();
    expect(pipe).toBeTruthy();
  });
});
