import { FilrepresentantePipe } from './filrepresentante.pipe';

describe('FilrepresentantePipe', () => {
  it('create an instance', () => {
    const pipe = new FilrepresentantePipe();
    expect(pipe).toBeTruthy();
  });
});
